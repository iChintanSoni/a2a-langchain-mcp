apiVersion: v1
kind: Service
metadata:
  name: mcp-server
  namespace: a2a-system
spec:
  selector:
    app: mcp-server
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30003
  type: NodePort
