apiVersion: v1
kind: Service
metadata:
  name: mcp-server
  namespace: a2a-system
spec:
  selector:
    app: mcp-server
  ports:
    - port: 5050
      targetPort: 5050
      nodePort: 30003
  type: NodePort
